Google Drive OAuth setup

For normal Google login, place your Desktop OAuth client JSON here and name it:

google_oauth_client.json

This is NOT a service-account JSON file. Service-account JSON remains optional in the app Advanced authentication section.

You can also set environment variable QUERY_BUILDER_GOOGLE_OAUTH_CLIENT_JSON to point to the OAuth client JSON file.
