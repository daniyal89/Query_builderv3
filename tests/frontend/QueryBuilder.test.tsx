/**
 * QueryBuilder.test.tsx — Tests for the Query Builder Page.
 *
 * Verifies:
 * - Table selection populates the column picker.
 * - Adding/removing filters updates the filter panel.
 * - Query execution renders results in the grid.
 * - Loading and error states are handled correctly.
 */

// TODO: Use @testing-library/react + MSW for API mocking
