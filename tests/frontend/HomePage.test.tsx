/**
 * HomePage.test.tsx — Tests for the Home Page.
 *
 * Verifies:
 * - PathInput renders and accepts user input.
 * - Connect button triggers the connection API call.
 * - TableList displays tables after successful connection.
 * - Error states are displayed to the user.
 */

// TODO: Use @testing-library/react + MSW for API mocking
